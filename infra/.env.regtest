# Dedicated regtest profile
ENV_PROFILE=regtest

# Las variables de entorno están alineadas con docker-compose.regtest.yml.
# Si ejecutas los servicios directamente, cambia hosts a localhost según corresponda.

# Service routing
# Gateway: allowed Origin header for browser clients (CORS)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

WALLET_SERVICE_URL=http://wallet:8001
TOKENIZATION_SERVICE_URL=http://tokenization:8002
MARKETPLACE_SERVICE_URL=http://marketplace:8003
NOSTR_SERVICE_URL=http://nostr:8005
AUTH_SERVICE_URL=http://auth:8000

# Database
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=tokenization
POSTGRES_USER=local_user
POSTGRES_PASSWORD=local_password
DATABASE_URL=postgresql+pg8000://local_user:local_password@postgres:5432/tokenization

# Redis
REDIS_URL=redis://redis:6379/0

# Bitcoin RPC
BITCOIN_RPC_HOST=c26z7zcu1avnvd1tdlfvv53r.cuboplus.caeher.com
BITCOIN_RPC_PORT=443
BITCOIN_RPC_URL=https://c26z7zcu1avnvd1tdlfvv53r.cuboplus.caeher.com/
# El endpoint remoto exige credenciales vÃ¡lidas de JSON-RPC por POST.
# Sustituye estos placeholders por las credenciales reales del nodo.
BITCOIN_RPC_USER=local_rpc
BITCOIN_RPC_PASSWORD=local_rpc_password
BITCOIN_NETWORK=regtest
BITCOIN_RPC_REQUIRED=true

# LND gRPC
LND_GRPC_HOST=i75jc9d7gbcmaqsdw3emjs8p.cuboplus.caeher.com
LND_GRPC_PORT=10009
LND_MACAROON_PATH=/app/infra/lnd/remote/admin.macaroon.hex
LND_TLS_CERT_PATH=/app/infra/lnd/remote/tls.cert
LND_TLS_SERVER_NAME=i75jc9d7gbcmaqsdw3emjs8p.cuboplus.caeher.com
LND_GRPC_REQUIRED=true

# Elements / Liquid RPC
ELEMENTS_RPC_HOST=elementsd
ELEMENTS_RPC_PORT=7041
ELEMENTS_RPC_USER=user
ELEMENTS_RPC_PASSWORD=pass
ELEMENTS_NETWORK=elementsregtest
ELEMENTS_WALLET_NAME=platform
ELEMENTS_RPC_REQUIRED=true

# Nostr
NOSTR_RELAYS=wss://relay.damus.io,wss://nos.lol
# NOSTR_PRIVATE_KEY=

# Auth
JWT_SECRET=local_jwt_secret
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
TOTP_ISSUER=RWAPlatform-Regtest

# Initial admin created by manual db bootstrap
INITIAL_ADMIN_EMAIL=admin@regtest.local
INITIAL_ADMIN_PASSWORD=RegtestAdmin123!
INITIAL_ADMIN_DISPLAY_NAME="Regtest Platform Admin"

# AI
OPENAI_API_KEY=

# Runtime
LOG_LEVEL=DEBUG
MARKETPLACE_ESCROW_WATCH_INTERVAL_SECONDS=30
MARKETPLACE_ESCROW_FEE_RESERVE_SAT=5000

# Alembic: auto-clean alembic_version if it points to a revision no longer
# present in the script tree (e.g. after a migration squash). Safe for
# regtest/local only; must remain disabled for staging/beta/production.
ALEMBIC_RESET_STALE_VERSION=true

# Wallet Security (dev-only; never use this hex key in staging/beta/production)
WALLET_ENCRYPTION_KEY=00112233445566778899aabbccddeeff00112233445566778899aabbccddeeff

# Alerting
# ALERT_WEBHOOK_URL=http://localhost:9000/hooks/regtest
